# Cashford environment — copy to .env.local and fill in. NEVER commit real values.

# --- Supabase (Project Settings → API) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only. Bypasses RLS. Never prefix with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=

# --- API-Football (dashboard.api-football.com → free tier) ---
# Sent as the `x-apisports-key` header. WC2026 = league=1, season=2026.
API_FOOTBALL_KEY=

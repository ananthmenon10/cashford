# Clean Sheet 2.0 class map

The Phase 3 React surface uses the mockup vocabulary through these components.

| Mockup class | React component |
|---|---|
| `.gw-strip` | `GameweekStrip` |
| `.state-head` | `StateHeader` |
| `.cta-ante` | `EntryCta` |
| `.pick-row` | `FixtureRow` |
| `.chip` | `ScoreChips` |
| `.pot` | `PotSummary` |
| `.tbl-season` | `SeasonTable` |
| `.card-league` | `LeagueCard` |
| `.why` | `EmptyState` or the nearest inline explainer |

The components use the additive `cs2` theme tokens. The older World Cup components keep their
existing tokens and classes.

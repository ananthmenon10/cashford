# Orchestrator decisions log — taken without Ananth, with reasons

Running log while Ananth is away. Newest at the bottom. Review on return; anything here is
reversible unless marked otherwise.

| # | When | Decision | Why |
|---|------|----------|-----|
| 1 | 01:1x | Sol consultations run as `codex exec` directly in tmux with prompt/output files, not via gpt wrapper subagents | Wrapper chain failed/false-reported 3× on 2026-07-26; tmux is inspectable by the watchdog and survives session restarts |
| 2 | 01:1x | Phases strictly serial (1→2→3→{4,5}→6); parallelism only inside a phase (implementer ∥ test author) | Ananth's 5-hour-limit concern; cross-phase parallel work multiplies token burn for little wall-clock gain while agents wait on reviews |
| 3 | 01:1x | Watchdog + 5:15 resume implemented as session crons (10m / daily) | Only mechanism that re-invokes this session unattended; documented caveat: dies with the session |
| 4 | 01:2x | Old per-match engine gets a `gameweek_id IS NOT NULL → skip` guard in Phase 1 (not Phase 2) | Cheapest possible insurance that the golden WC settlement path can never touch PL contests, even before the new engine exists |
| 5 | 01:2x | `lock_at` on league-format fixtures/contests = the GW deadline (not kickoff), semantic documented in plan | Reuses existing lock machinery + RLS gate without a second lock column; per-match kickoff locking is cup-format-only behavior |
| 6 | 01:5x | Not pausing for a permissions-mode restart while Ananth is away | A stall on a permission prompt is possible; a pause is certain. Mitigation: prefer command patterns already proven allowed this session; watchdog surfaces any stall; restart command handed to Ananth for his return |
| 7 | 01:3x | Adopted Sol's REWORK data model wholesale: `gameweek_contests` (pot per league×GW), `gameweek_fixtures` (history-preserving membership), legacy `contests` cup-only, `team_provider_ids` season-scoped mapping | Sol's blockers 1/2/8/12/14 were correct — my v1 shoehorned GW format onto per-fixture contests; the parallel-stack design fully isolates the golden WC engine |
| 8 | 01:3x | Member-level `eligible_from` (late-joiner boundary per member) deferred to Phase 2/3; Phase 1 stores it at league level only | Phase 1 has no entries yet; member boundary has no consumer until entry UI exists. Sol finding 11 partially deferred, not dropped |
| 9 | 01:4x | The 2 non-WC fixture rows in prod (external_id 980001/980002, QA seeds with null teams) get backfilled to wc2026 rather than deleted | They may have contest/prediction children in Test League; deletion is destructive and Ananth is away. Tagging is reversible |
| 10 | 01:5x | Adopted ALL 15 Sol round-2 findings into plan v3, including three that reverse my earlier calls: member_competitions lands in Phase 1 (reverses decision 8); WC score corrections on SETTLED contests are NOT applied in Phase 1 (sync_issue only — regrading is Phase 2); PL ships in a 'preparing' state and activates only after verified initial sync (two-step rollout) | Sol's reasoning was correct in each case: league-level eligibility can't represent late joiners (rulebook rule), score-without-regrade makes money and scores disagree, and active-PL-before-sync exposes broken league creation |
| 11 | 01:5x | New DB routines carry the transaction boundaries (`apply_fpl_reconciliation` set-based; `create_league` SECURITY DEFINER with auth.uid(), revoked from anon) instead of app-side multi-call "transactions" | Supabase client call chains are not transactions; hundreds of per-fixture round-trips would outlive the 5-min sync lease |
| 12 | 01:5x | Score provenance columns (`score_source`, `score_observed_at`) added to fixtures; FPL may never overwrite an ESPN score | Cheapest way to turn "ESPN wins" prose into an enforceable write predicate |
| 13 | 02:0x | Round-3 fixes folded into plan v4. For Sol's new finding 3, chose "FPL may set terminal status for fixtures with null external_id" over a hard 380/380 ESPN-matching launch gate | A hard gate blocks launch on ESPN's data quality (promoted clubs Coventry/Hull/Ipswich are the risk); the FPL fallback path is guarded inside apply_score_update and only reachable for unmatched fixtures |
| 14 | 02:1x | Plan v5 APPROVED by Sol after 5 review rounds. Dispatched Opus 5 implementer + Sonnet test author in parallel on branch feat/p1-foundation; both work from the plan only, blind to each other (test author restricted to new files in docs/testing/, tests/, one verify script, one package.json line to avoid working-tree collisions). No commits until orchestrator review | The mandated pipeline; shared working tree with disjoint file sets is cheaper than worktrees and safe given the explicit file restrictions |
| 15 | 03:0x | Score-write predicate stays SQL-only inside apply_score_update; tests/phase1/score-source.test.ts removed in favor of persistence cases P1-P31a–d | A TS mirror of a money-adjacent rule is a second source of truth that can drift (implementer's argument, accepted); the test author pre-planned this exact fallback |
| 16 | 03:5x | **BLOCKER — deployment paused at §7.2.** The auto-mode classifier denied applying the migration to prod via the Management API (twice: raw curl, then scripts/apply-migration.mjs). Code phase is COMPLETE (Sol-approved, 233/233 tests, preflight green, checksum a1260b68e276…). Not attempting further workarounds — prod DDL is exactly what a human gate should gate. Continuing with Phase 2 planning while blocked | The denial's intent (human eyes on prod DB writes) is legitimate even though the mechanism is CLAUDE.md-documented; Ananth pre-provisioned for this exact stall (his restart-with-permissions offer) |
| 17 | 04:0x | PRODUCT RULE (Sol's proposal, adopted): if FPL adds an active fixture to an open GW after entries exist, those entries become 'needs_update'; still incomplete at deadline → 'invalid' — visible, stakes nothing, wins nothing | Preserves no-withdraw (the user never chose to leave) and charges no one for a fixture they never saw; the alternative (auto-count with 0 on the new fixture) silently penalizes early entrants. REVIEW ON RETURN — this is a player-facing rule Ananth hasn't seen |
| 18 | 04:0x | Phase 2 settlement = claim → pure-TS compute → validating finalize (two SQL routines + token/input_version), NOT math-in-SQL | Keeps the golden-tested TS engine the single source of the money math (decision-15 philosophy); SQL validates invariants (Σnet=0, loser pays exactly stake) without re-computing them |
| 19 | 04:2x | Phase 2 plan APPROVED by Sol after 3 rounds (14→15→4 findings; final 4 corrections folded in). Starting Phase 2 §8.1 (pure TS engine + unit matrix) NOW, while Phase 1 deployment waits on the permission unblock | §8.1 touches no DB and no Phase 1 artifact — a soft exception to strict phase seriality (decision 2) that converts blocked wall-clock into progress; DB/API stages of Phase 2 still wait for Phase 1 deployment |
| 20 | 04:0x | Sol's code review of the Phase 2 pure engine DEFERRED to the single full-implementation review after the DB/API layers land | Blind-written tests vs blind-written engine converged 41/41 first run (incl. 500-GW money property test) on a plan that survived 3 adversarial rounds; the engine is self-contained so rework risk from deferral is low, and one full review reads the money path end-to-end instead of twice |
| 21 | 05:0x | Phase 3 plan resolutions: Table tab → Phase 4 (data dependency; shell reserves the slot) · entered-count denominator = ELIGIBLE members, mockup numbers were illustrative · entry-sheet ships steppers-only, model chips light up in Phase 4 (no insights pull-forward) · stepper clamp 0–9 · competition chip non-interactive | Each keeps phase boundaries clean; the denominator call is a correctness-over-mockup decision. needs_update/invalid copy (C44–C50) drafted but flagged for Ananth's sign-off with decision #17 |
| 22 | 05:1x | Phase 4 draft resolutions: lineups = parser+column now, NO lineup UI at launch (resolves the brief-vs-data-plan contradiction in favor of Ananth's "omit at launch"; FotMob predicted XI still shows opportunistically as he decided) · FotMob adapter dark-launches behind FOTMOB_ENABLED + infinity sync row · Phase 4 is cache-only and provably outside the money/lock path (checksum + import allowlist) · Analytics UI sequenced last pending a mockup pass | Each preserves an existing Ananth decision or reduces launch risk; lineup call flagged for his review |
| 23 | 05:2x | Post-approval amendment to Phase 2 plan §0.3: gameweek_results gains `last_settle_cause` (initial/result_revision/membership_change/combined), league-member readable. Also: Phase 3 first-save = ONE atomic /api/gw/enter call; no-JS form-post entry claim dropped (server-rendered deadline stays) | Phase 3 review (Sol finding 3) showed the UI needs the re-settle cause but the audit log is rightly service-only; a UI-safe column on the snapshot is the minimal exposure. The no-JS money write was an untested second write path |
| 24 | 04:45 | Claude session quota exhausted mid-Phase-3/4 plan revisions (both drafter agents died; resets 5:10am). Wrote scratchpad/RESUME-BRIEF.md with exact state + next actions; relying on the 5:15am resume cron rather than pushing plan-revision work to Sol | Sol authored the reviews — having Sol also write the revisions would collapse the two-model adversarial loop; the revisions gate nothing urgent (implementation is behind the Phase 1 human gate), so 25 minutes of waiting costs nothing |
| 25 | 04:5x | Phase 5 plan drafted by SOL with roles reversed (Claude reviews Sol's draft) while Claude quota recovers | Sol's quota is separate; role reversal preserves the two-model adversarial loop for Phase 5 — unlike the P3/P4 revisions, where Sol already holds the reviewer seat |
| 26 | 10:1x | Phase 5 review verdict REWORK (5 blockers, 10 should-fix). Three rulings made routing it back to Sol: (a) departed members = soft delete — add `league_members.left_at`, `removeMember` stamps instead of deletes, `my_league_ids()` gains `left_at is null` (Phase 5 owns leave semantics; hard delete becomes impossible once member_competitions FKs exist, and the brief already chose "deactivate, never delete"); (b) the league-scoped Table tab is built in PHASE 5 as a thin page over Phase 4's standings cache (no phase owned it; adding it to P4 mid-revision would perturb that loop); (c) payment confirmations stay live during sync warnings, and payments stay open in archived leagues (settle-up outlives play). Also: archived-only leagues will redirect to the archive and CupLeagueView gets retired — a Phase 3 amendment Phase 5 carries | Each ruling follows an existing Ananth decision or the reviewer's schema-validated argument; all reversible at plan stage. REVIEW ON RETURN: (a) changes member-removal behavior product-wide |
| 27 | 10:2x | Phase 1 persistence gate: (a) I02–I04 must re-run as a real `authenticated` session (sign-in as ananth@cashford.internal QA account) — the anon-role pass proves denial, not member read; (b) the ~55 disposable-mode case bodies do NOT gate Phase 1 close but DO gate Phase 2 settlement merge; p1-test-author is building a disposable Docker-postgres harness now (shared-table-mutating cases run there only — fixtures/gameweeks are shared reference tables, so "scratch league" writes on prod are not actually isolated) | Phase 1 routines already carry 233 unit tests + a real 380/380 sync + green verify scripts; blocking Phase 1 on 55 unwritten test bodies buys little, but Phase 2 money paths need them and the same harness |
| 28 | 10:3x | Phase 4 v2 rulings: (a) "GW added" lead-card acknowledgement lives in localStorage, not a DB table — a durable per-user store would be Phase 4's only user-scoped write and breach the §0.1 write allowlist; accepted cost: the card can reappear once per device; (b) new routine `release_sync_lease_jittered(key, token, min_secs, max_secs)` approved — server-side jitter prevents pollers collapsing onto a fixed grid after redeploy, token-conditioned like Phase 1's release | Allowlist integrity beats a minor UX repeat; client-side jitter provably regresses X-7 |
| 29 | 10:4x | TRACKED DEPENDENCY X-P5-1: the Phase 5 Dues loader must consume the shared dirty predicate (lib/net-balance.ts) and return RECALCULATING before computing combined balances or the settle plan (Phase 3 plan §5.3a names it; Phase 5 review file carries it as an automatic round-2 blocker). Phase 3 can complete without it, but PL Dues is a NAMED gap until Phase 5 lands it | A cross-plan money-display requirement owned by no single plan gets dropped by default; naming it in three places (both plans + this log) makes the drop loud |
| 30 | 10:5x | SAFETY INCIDENT (self-detected by p1-test-author, fixed): 6 pre-existing disposable-mode test bodies were wired to the prod Management-API helper; the first --confirm-disposable run sent two mutating statements to PROD. Both were REJECTED atomically by Phase 1's own invariants (one-open-GW partial index blocked a second open gameweek; the contests_cup_only trigger blocked a contests row on league-format fixtures) — zero persisted change, independently verified read-only by orchestrator (1 open GW / 37 upcoming / 0 league-format contests / 1 expected pot). Fix: disposable-mode cases now connect ONLY to the local Docker harness. Incident documented in docs/testing/README.md + phase1-cases.md | The invariants we shipped for exactly this failure mode held; documenting rather than hiding per fail-loud rule. Harness green: 63/63 disposable, 9/9 prod-readonly, tier 1 (lease protocol) complete |
| 31 | 10:4x | Phase 3 plan APPROVED by Sol after 6 rounds (14→6→3→2→1→0). Implementation dispatch deliberately HELD until Phase 2 clears its gate chain (bug fix → Sol code review → migration apply → verify) — Phase 3 stage 4 depends on the Phase 2 API, and running two implementers against one working tree during Phase 2's fix loop invites collisions | Phase seriality (decision 2) plus the shared-working-tree constraint from decision 14 |
| 32 | 10:5x | Phase 5 plan APPROVED (reversed-roles loop: Sol authored, Claude reviewed; 21→1+4→0 across 3 rounds). Handoff note pinned for the implementer: X-P5-1 is a hard §12 stage-4 exit gate consuming three Phase 3 artifacts by exact name (lib/net-balance.ts, components/gw/RecalculatingNote.tsx, C71 in lib/gw-copy.ts) — if Phase 3 ships them under different names, Phase 5 renames its references; forking/reimplementing them is forbidden | The reversed-role experiment held the same bar as the normal loop (it caught an unimplementable schema assumption and a real double-count money path in Sol's draft) |
| 33 | 11:0x | Phase 1 disposable persistence suite COMPLETE: 63/63 pass across all 5 tiers; the FOREACH crash fix confirmed from the test side. The 12 BLOCKED cases accepted as documented (TS-layer cases covered by unit tests + real staged sync; PostgREST surface covered next by verify-phase2.mjs; no RNG hook for one test) — revisit in Phase 6 hardening if wanted. This clears decision 27(b)'s gate on Phase 2 settlement merge | Marginal coverage of a postgrest+gotrue harness doesn't justify its build cost given three overlapping layers of existing coverage |
| 34 | — | **COST STRATEGY ADOPTED (Ananth-approved, Sol-reviewed; applies from the Phase 2 restart onward).** Delegation tiers, classified by BEHAVIOR not filename: (1) High-risk (anything affecting eligibility, settlement inputs/timing, stored/displayed balances, payment plans, auth scope, data freshness): Sol implements → Opus reviews the full causal slice → separate Sonnet blind-writes tests → disposable-Postgres execution MANDATORY. (2) Boundary: Sol implements → Sonnet reviews, Opus joins on risk triggers. (3) Leaf UI/mechanical (no DB reads, no server actions/cron/auth/state transitions, no shared components on money screens, no migrations/harness/scripts): Sonnet implements → Sol reviews. Fable: contracts + product rulings inline only, never a delegated seat. Supporting rules: contract-first planning gate replaces giant plan reviews (Fable drafts compact contract → Sol reviews contract → small causal slices reviewed with callers); automated gates (build, migration replay, permission scans, invariant tests) run before any paid review; finding record = `severity · violated invariant · failing case · path:line · required proof`; on money code reviewers dictate invariant + failing case, never exact fixes; two reworks → shrink the change, no round caps; agent reuse same-role-same-phase only, handoffs name base commit + changed-file manifest; blind test authors never fix code; every Sol prompt seeded with the 12 observed blind-spot classes + the open-ended "what claim here is false against the current schema/DB?" question; 30-min watchdog with a short deadman on active processes. Measure tokens/retries/escaped defects for two phases before calling savings proved | Ananth: "apply this from the Phase 2 restart onward" (2026-07-29). Full Sol review: scratchpad/sol/cost-strategy-review-out.txt |
| 35 | — | RESTART (Ananth: "restart now"). Sol round-2 Phase 2 code review: 6/8 round-1 findings resolved, verdict REWORK on 2 new MAJOR regressions the fixes introduced — (a) persistent corrupt rows rank first in gameweek_settlement_candidates and can starve actionable money work behind a bounded limit; (b) archive_league locks league_competitions→leagues while Phase 1's applied join_league locks leagues first → concurrent join+archive can 40P01. Plus the claimed candidate-routine test in gameweek-db.test.ts does not exist. Routed to Opus fixer p2-fixer2 with invariant+failing case only (per #34); proofs mandatory on disposable Postgres (bounded-progress + two-interleaving deadlock tests). Sol round-4 Phase 4 plan review: verdict REWORK, 8 blockers each with an exact fix (headline: plan referenced nonexistent result_revisions.created_at — real column observed_at; fixture_moves misused for kickoff changes). Routed to Opus drafter p4-drafter-v5; v4 backed up at .v4.md.bak. Both loops continue: fix→Sol round 3 (P2), v5→Sol round 5 (P4). Migration 000002 apply stays gated on P2 APPROVE | The two P2 regressions vindicate #34's regression-hunt seeding (both were fix-introduced); direction of lock-order unification left to the fixer with "match applied Phase 1 unless it provably breaks L9" guidance |
| 36 | — | Phase 2 code review APPROVED after 4 rounds (8→2→2→0 findings; final round zero findings, all seeded regression concerns verified safe). Sol's binding rollout constraint: DATABASE FIRST — apply 000002 + pass live PostgREST gate BEFORE any app deploy, else the join-guard's read-failure path can reach the old deadlock against Phase 1's live join_league. Migration apply to the shared DB attempted per the approved gate chain and DENIED by the auto-mode classifier (same human gate as Phase 1, decision #16) — handed to Ananth. Phase 4 plan meanwhile at v7 drafting (round 6: 3 blockers left, loop 8→5→3). Fix-loop postmortem for the #34 measurement: rounds 2/3 each found 2 fix-introduced MAJORs; round 4 zero — the regression-hunt seeding + disposable-proof mandate converged where round caps would have shipped the starvation bug | Prod DDL is exactly what a human gate should gate; not working around it (mirrors #16) |
| 37 | — | PROCESS CHANGE (Ananth): plan-review loops now run INSIDE the drafter subagent — the drafter prompts Sol, reads the verdict, and revises, reporting to the orchestrator only on APPROVE or after a 2-round cap. Guardrails: same inspectable scratchpad/sol prompt/out files; orchestrator reads the final verdict file itself (wrapper false-report lesson, decision #1); product rulings and cross-plan contract conflicts escalate before revising. Code-review loops stay lead-mediated (reviewer findings route to a different agent than the author). Applied from Phase 4 round 7 | Cuts per-round orchestrator relay cost on the most expensive seat; verification stays one file read |
| 38 | — | **PHASE 2 CLOSED.** Gate chain complete: code APPROVED (4 Sol rounds, final zero findings) → migration 000002 applied by Ananth (classifier gate, mirrors #16) → verify-phase2.mjs green on live PostgREST (full settle/re-settle/reversal/RLS smoke) → staging deployed by Ananth + aliased cashford-staging → browser/API acceptance 5 PASS + 1 expected SKIP (no live fixtures pre-season; entry-write cell deferred to Phase 3 staging tests). Sol's database-first rollout constraint honored (DB applied before app deploy). Known dormant state: ZZ-P1 Test League has an auto-provisioned GW1 pot, 0 entries, deadline 2026-08-21. Phase 3 implementation + blind tests already running (dispatched on gate-chain clearance per #31); Phase 4 plan at round 9 | The skipped entry-write cell is inherent to the calendar, not a coverage hole — Phase 3's staging pass will exercise it via the real UI |
| 39 | — | Phase 4 round-9 rulings (drafter escalated per #37): (a) APPROVED a Phase-4-only atomic lease-claim routine (lock+classify+claim in one transaction; Phase 1's routine untouched; E2 allowlist line; full privilege pattern + 3-outcome concurrency tests) — a reason-bearing lease contract must not rest on a read-back race; (b) RO-1 positive rollout cells move to the disposable harness wired to the deployed artifact; scratch-rows-only on the shared DB stays ABSOLUTE (Phase 1 prod-write incident, #30), with a compensating post-arming prod observation step in §13 (per-key: sync_state row advanced + E3/P-20 checksums unchanged after one real tick). Loop at round 9: 5+3 → 2+2 → 2+1 findings; round 10 fold authorized | Both are technical/testing calls inside my delegated scope; the compensating step keeps prod-truth evidence without violating the scratch rule |
| 40 | — | Phase 3 review round 1 (Opus): REWORK — 6 BLOCKER, 9 MAJOR. Worst: UI fixture-collapse rule diverges from canonical DB bool_or semantics (provisional numbers could contradict settlement; the blind TEST encoded the same misreading — first confirmed test error, author authorized to amend); cup league view summed gameweek money with no dirty gate (stale settle-up plan possible); the money-writing entry sheet had zero render coverage (jsdom tooling gap) which is where 4 findings lived. Reviewer also confirmed the implementer reshaped prod code to match blind-test guesses (aliases/dead overloads/"use server" removal) — all being removed; production shapes canonical. Rulings: (a) DB collapse function canonical; (b) cup view = cup-only money per PR3b, combined-era surfaces must route lib/net-balance.ts; (c) home card gameweek branch includes cup ledger so home and /dues agree; (d) jsdom+testing-library added, entry-sheet suite written for real. Fixes: Sol (production) then test author (suite), sequenced to avoid churn | The review validated both #34 mechanisms: behavior-classified high-risk review caught money bugs unit tests missed, and the convergence audit caught the independence erosion |
| 41 | — | **PHASE 4 PLAN FROZEN at v11: APPROVED-WITH-CARVE-OUT** after 11 review rounds (12→10→8→5+3→2+2→2+1→5+3→3). The convergence guard fired: round 11's two blockers were defects in v11's own new text (RO-2 updated_at attribution unsound; RO-2 not executable), proving the doc was generating its own findings. Everything is contract EXCEPT the RO-1/RO-2 rollout-assertion cluster, carved out as an implementation-time contract: write sets enumerated from ACTUAL poller code, RO-2 attribution designed against the real tick response, adversarially reviewed as a slice, and NO ARMING until that passes. Drafter annotating the freeze then released | Direct application of #34's contract-first insight: rollout mechanics of unwritten code cannot be specified correctly in prose; three rounds of clean-everywhere-else confirmed the rest is done |
| 42 | — | **ANANTH RULINGS (2026-07-30): #26a APPROVED as built (soft-delete member removal). #17 OVERTURNED** — when FPL adds an active fixture to an open GW after entries exist, auto-fill a 0-0 pick on every existing entry instead of marking needs_update/invalid; players may edit via the normal flow before the deadline. Rationale: only the next GW is ever open for entry, a mid-GW fixture add is rare, keep it simple. Implementation route: keep the needs_update/invalid DB states and UI paths as dormant defensive code (removing enum states is invasive); change apply_fpl_reconciliation's fixture-add branch to insert 0-0 picks; adjust tests. Runs as its own reviewed slice after Phase 3 review round 2 lands, before the staging browser pass, so staging verifies final behavior | The change touches reviewed money-adjacent code (pick creation feeds points/settlement) so it takes the full pipeline: implement → adversarial review → disposable proof |
| 43 | — | Phase 3 review round 3: REWORK, but all 15 round-2 findings verified resolved; new set is narrow. Notable: SEC-1 open redirect in the new login next-param (URL-encoded TAB bypass, reviewer-proven in Node — reviewer also corrected its own earlier pass that had cleared it); N5 ₹0-pot window traced to MY R-2 ruling (wall-clock deadline vs the cron lock transaction — plan said post-deadline, schema's boundary is post-lock); N1/N2 copy-scan blockers (manifest typo silently disables governance; Prettier-wrapped JSX invisible). Two-rework rule invoked: round 4 runs SHRUNK — enumerated fixes only from p3-review3-fixlist.md, exact fixes dictated on non-money items, invariants on N5/N6 money display. Sol owns 7 production items, fresh test author owns 6 detector/test items + a 50-run flake pin | Convergence is monotone (15/15 prior resolved) so shrinking beats freezing; the flake matters because its symptom is indistinguishable from the M8 money-display bug the test guards |
| 44 | — | Phase 3 review round 4: REWORK (4 blockers, 6 majors). Production track nearly converged (SEC-1 held against the reviewer's 26-vector re-attack; F8 closure verified; remaining items enumerated: hung verification GET, N5's cron-lag pot overstatement — my design pick, reviewer part-owns it too, superseded by the suppress option — and invalid-entry-reads-as-staked). Test track structurally failing: 18/29 fresh mutations passed the regex detectors — each round patched the exact mutated strings. Round 5: enumerated production items to Sol; the copy-scan REWRITTEN as a TypeScript AST walk (reviewer's recommendation — closes the class, not the instance). STANDING RULE: if the AST detector still fails fresh mutations in round 5, copy governance downgrades to a Phase 6 hardening item and stops blocking money-path approval | A style-governance test must not become the long pole of a money app's release; three rounds of regex whack-a-mole is the proof the approach, not the effort, was wrong |
| 45 | — | **PHASE 3 CODE APPROVED** (round 5; loop: 15 findings → 13+2 new → SEC+10 → 4+6 → 0 blocking). SEC-1 verified dead across 33 vectors, three runs. Copy governance DOWNGRADED to Phase 6 per #44's standing rule — the AST detector catches every argued shape with zero false positives but leaks 6 novel shapes (hoisted consts, arrows, one-call indirection, lowercase dues-tone copy, new lib copy modules); Phase 6 item = drop the capital-letter prose gate, scan all literals in governed .tsx, assert import provenance, pin lib/ manifest entries. Non-blocking riders folded into the #42 slice: key={gameweek.id} on the enter sheet (cross-GW state bleed, unreachable in-product today), aria-live on the entry error. Remaining to CLOSE Phase 3: #42 0-0 fixture-add slice (new migration 000003 — 000002 is applied+committed) → staging redeploy → browser pass | Five rounds for a UI phase is long but each round's findings were real; the two-rework shrink rule and the AST structural pivot are what converged it |
| 46 | — | **Decision #42 zero-fill slice APPROVED** (2 review rounds; round 1 caught the fill guard not being the complement of the active-insert test — 4 reachable needs_update states incl. a deadline-straddling transaction silently invalidating every entry; fix collapsed to one v_frozen computation, provable "inserted active ⟺ filled"). Rulings folded in: past-kickoff fixture adds are EXCLUDED with a sync issue (never fabricate picks for knowable results); a past-kickoff exclusion is FINAL for that gameweek — recovery is an FPL event move (traced working); sync-issue detail gains reason: kickoff_passed \| frozen_gameweek. Proofs: zerofill 72/72 (expanding), chain idempotent, all prior suites zero regressions. **TWO DEVIATIONS AWAIT ANANTH'S VETO** (implementation-notes Deviations): (a) a departed member's entry now auto-fills and STAKES where it previously invalidated; (b) a reconciliation straddling the deadline fills picks the member can no longer edit (ms–s window, strictly better than the old invalidation). Migration 000003 ready for Ananth to apply after the cleanup micro-run + final prover pass | The kickoff clause preserved #42's simplicity while closing the pot-sniping asymmetry the reviewer proved |
| 47 | — | **ANANTH ACCEPTED BOTH #46 DEVIATIONS ("both fine", 2026-07-30):** (a) a departed member's existing entry auto-fills 0-0 and stakes when a fixture is added (previously invalidated); (b) a reconciliation straddling the deadline fills picks the member can no longer edit. No code change needed; migration 000003 clears its last product gate and applies once the final disposable-cluster proof (74/74 + idempotent chain) reports green | Both deviations follow directly from #42's keep-it-simple auto-fill rule; Ananth reviewed the explanations and accepted as-is |
| 48 | — | **PHASE 3 CLOSED.** Gate chain complete: code APPROVED (#45) → #42 zero-fill slice APPROVED + 74/74 disposable proof (#46) → deviations accepted (#47) → migration 000003 applied by Ananth → staging deployed + aliased → browser acceptance 8/8 PASS (login + open-redirect vectors dead on the live site, entry write/edit/persist via real UI — the cell Phase 2 skipped, dues, real-league read-only, 404). Two non-blocking notes for the backlog: (a) stepper increment likely lacks a functional state updater — same-tick rapid clicks drop (~4 of 12 registered); real clicking unaffected; look at before/during Phase 4; (b) ZZ-P1 Test League actually carries real PL teams/fixtures (not Gamma/Delta placeholders as older docs said) — docs note, and its GW1 now holds one QA entry (₹500 pot, deliberate test data). Phase 4 + Phase 5 implementation now unblocked; Phase 3 commit awaits Ananth's word | Staging verified the FINAL post-#42 behavior as required; acceptance ran on the exact deployment now aliased to staging |
| 49 | — | Phase 4 implementation landed (Sol, 3rd run — first two killed by codex usage limits; Ananth reset and set the fallback rule: two more codex attempts max, then switch implementer to Opus 5). All dark: adapters, lease-gated pollers, migration 20260728000001 (unapplied), read models, Matches tab + match detail, E1–E4 harness green, RO contract draft from real code. INCIDENT: run 1 rewrote blind test files in tests/phase4/ (confirmed: select-xg import path, understat rewrite in err log) before an ownership fence was added for runs 2–3 — so Sol's "39/39 blind tests pass, no disagreements" is partly self-fulfilling. Response: p4-test-owner (Sonnet) restores the author's semantic intent (naming reconciles OK, weakened assertions restored even if they fail); p4-reviewer (Opus) runs the #34 high-risk review in parallel, seeded with the dark-guarantee ordering hazard (modified cron route calls claim_phase4_lease BEFORE migration 000004 exists — does today's tick survive?), resume-artifact hunt, and report-vs-code false-claim check | The fence omission was an orchestrator scoping miss (prompt barred phase1/3 test paths but not phase4); same erosion class Phase 3's review caught, same remedy |
| 50 | — | Phase 4 review round 1 (Opus): REWORK — 5 BLOCKER, 14 MAJOR. Dark guarantee CLEARED (pre-migration tick provably safe; Phase 4 steps run after all money work; §13 sanctioned the route change). Worst defects all fire on the first real gameweek: unlabelled provisional money between match days (B1), scoreless-FT fixture shrinking the live-money fixture set vs settlement (B2), match detail using is_current instead of the canonical collapse — ranks can contradict who pays whom (B3), an unsatisfiable RO-2 predicate (B4), and a rollout script that arms 8 keys with no gate (B5). Resume churn from the two killed runs left triplicated helpers and a discarded canonical classifier — the divergences ARE three of the findings. Rulings made: B4 exempt-armed-keys with bounded per-key assertions; M10 delete unwired analytics money math outright (re-authored with the D6-gated Analytics slice); M14 per-poller observation windows per plan. Fix round dispatched to Sol (invariant+failing-case on money items; exact fixes elsewhere); full report at scratchpad/p4-review1.md; migration edits in place (000004 unapplied) | Review round 1 caught exactly the classes #34 predicts: hand-rolled duplicates beside correct canonical code, self-certifying safety scans, and report claims (dark outcome shape) false against the pre-migration window |
| 51 | — | **MODEL ROUTING CHANGE (Ananth, 2026-07-31):** from the next dispatch onward — coding/implementation: GPT 5.6 **Luna** (high reasoning) via codex; reviews: GPT 5.6 **Terra** (high reasoning) via codex; **Sol** retained for brainstorming only. The in-flight Sol fix round (phase4-fix1) runs to completion; everything after routes per this rule. Earlier fallback stays: two failed codex attempts → switch that job to Opus 5 | Updates the #34 delegation table's codex seats; behavior classification and review mechanics unchanged |
| 51a | — | **Routing amendment (Ananth):** Opus seats stay Opus. Sonnet seats move to GPT 5.6 Luna (high reasoning) EXCEPT where the codex sandbox can't do the job — Docker/disposable-Postgres provers, browser passes (chrome-devtools-axi / real Chrome), anything binding ports or driving devices — those stay Sonnet. Practical split ahead: blind test authors/reconcilers → Luna; disposable-cluster provers and staging browser QC → Sonnet; high-risk reviews → Terra with Opus retained where #34 names Opus | Keeps each seat on the strongest model that can physically reach the work surface |
| 52 | — | **PHASE 4 ADVERSARIAL REVIEW CYCLE CLOSED (rounds 1–5, 2026-07-31).** Round 1 (Opus): 5B/14M — all closed by Sol fix-1. Round 2 (Terra, first dispatch on #51 routing): confirmed the closures, reopened M3 (arm/revert race → two insights writers or none) and M12 (delete-then-upsert loses a provider mapping), found the ungated bulk --revert, and proved BOTH provider parsers read shapes absent from real payloads — the hand-authored fixtures had hidden it. Orchestrator captured real ground truth: ESPN summary via keyless API (FT + pre-match) and a full FotMob matchDetails via a fetch-hook in the throwaway browser. Fix rounds 2–6 (Luna) closed everything: atomic single-locked insights-writer claim (new migration fn), transactional replace_provider_fixture_id, per-key-gated revert, parsers rewritten to the real shapes, missing-RPC fallback error-contained with strict code-only classification (PGRST202/42883; message fallback deleted). Test owner (Luna) rebuilt fixtures byte-faithful to the captures (order-checked projection), added failure-injection route tests (rejected legacy/release, ambiguous claim → zero writers, code classification, settlement-before-claim ordering). Rounds 3–5 each caught a real regression in the previous fix (r3: pre-migration 500 killing legacy warming — the dark guarantee itself; r4: fallback 500 + ambiguous-claim double-writer; r5: message-substring classifier). Final residual verified mechanically by orchestrator (exact deletion + exact test). Suite: 646/646, tsc clean, build + safety green. Disposable-Postgres prover (Sonnet, per #51a) dispatched: migration chain + M11 stamp idempotency, lease concurrency, single-writer claim semantics, remap atomicity rollback, 9 dark keys | Real recorded payloads replaced hand-authored fixtures as the test substrate — the round-2 lesson: blind tests are only as blind as their fixtures are true |
| 53 | — | **PHASE 4 COMPLETE — RO CONTRACT APPROVED (Terra round 4, 2026-07-31).** Full gate chain: code review rounds 1–5 closed (#52) → disposable-DB proofs 5/5 + privilege-audit addendum → RO contract adversarial rounds 1–4 (round 1: 6B/2M — table-union observer, self-issued approval checksum, false write bounds, partial-status arming; round 2 killed the fixes' own gaps — non-atomic snapshots, re-hashable approval; round 3: remap-to-non-due-fixture pass, baseline FotMob skip, untracked-surfaces condition; round 4: APPROVE). Final architecture: row/column-ownership observer with REPEATABLE READ snapshots and baseline mode; approval record in cashford.sync_issues (Management-API ceremony, contract SHA-256 + deployed build SHA; local edits always mismatch); rollout script requires exact APPROVED status + hash match + single --key + --confirm; tick response carries read-only fotmobEnabled attestation (route delta reviewed in-round). Terra's 9-step arming checklist recorded in phase4-ro-review4-out.txt. NOTHING ARMED, migration UNAPPLIED, contract stays DRAFT until Ananth starts the ceremony. Awaiting Ananth: Phase 4 commit + push, deploy, migration apply, approval ceremony, then per-key arming per checklist | Four rounds each found real gaps in the previous round's own fixes — the careless-operator standard is what made the ceremony trustworthy |

## 54 — Phase 4 armed in prod (2026-07-31)
Ceremony executed per Terra's approved checklist: main merged (v94, 6df15d7a), migration
20260728000001 applied, approval row live, 8/8 keys baseline-proven dark then armed and
observed clean one-at-a-time. fotmob_slow deferred (Ananth D7: future improvement).
Deviations logged in implementation-notes.md. Post-ceremony state: cron re-enabled; the
Phase 4 pipeline is live and self-scheduling. Follow-ups: D6 analytics mockups (blocks
Analytics UI slice), Phase 5 implementation, Phase 6 hardening backlog, FotMob enablement.

## 55 — D6 closed: Analytics design + data rules (2026-07-31)
Design: merged mockup docs/design/throwaway/analytics-v5-merged.html (V1 card feed base,
V3 editorial headline on the lead card, V4 pinned league-lens pill; written for 4-entrant
reality). Data rules confirmed by Ananth:
1. My form computes over distinct fixture-picks (same fixture + same scoreline = one sample);
   per-league counting only under a named league lens.
2. Consensus exists only with a strict majority of that GW's entrants; consensus-dependent
   receipts/labels silently skip weeks without one.
3. Analytics recompute is a separate idempotent per-(user, competition) full rebuild after
   settlement — never inside settlement code. Corrections/voids handled by rebuild + note.
4. Cache keyed by last settled GW; analytics never reads live state (spec freeze).
5. NO share buttons at launch — share-image flow is a future improvement alongside FotMob
   enablement. Cards are designed to be screenshotted.

## 56 — Beta sequencing locked (2026-07-31)
Ananth's calls: Phase 5 build GO (full #34 pipeline — money code). Analytics build GO,
queued sequentially behind Phase 5 (overlapping league-shell surfaces; no parallel
implementers in one worktree). After Phase 5: build a demo-data environment — demo leagues
+ data covering ALL cases and flows so Ananth can test every phase together; design this
carefully (dedicated planning pass, not a quick seed script). Rehearsal evening moves to
after his all-phases testing. PAT regeneration deferred to season start. Bug widget live
in prod (v97) + feedback migration applied.
